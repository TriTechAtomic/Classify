import 'package:flutter/material.dart';

class DesktopInterface extends StatelessWidget {
  const DesktopInterface({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
